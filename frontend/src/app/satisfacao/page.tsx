import InstitutionalContentPage from '@/components/InstitutionalContent/InstitutionalContentPage';

export default function SatisfacaoPage() {
  return (
    <InstitutionalContentPage
      slug="satisfacao"
      breadcrumbLabel="Pesquisa de Satisfação"
      defaultTitle="Pesquisa de Satisfação"
    />
  );
}
